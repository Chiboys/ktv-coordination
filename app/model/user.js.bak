var Mongoose = require('Mongoose');
var Schema = Mongoose.Schema;
var User = new Schema({
	id:String,
	searchWord:String,
	plan:[{
			searchWord:String,
			index:[Number]
		}]

	},{
	versionKey:false
	});